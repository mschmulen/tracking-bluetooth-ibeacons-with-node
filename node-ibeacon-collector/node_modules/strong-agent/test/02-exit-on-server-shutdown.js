var should = require('should');

describe('Exit', function() {
	it('exits when the server shuts down', function() {
		require('../').profile('deadbeef', 'deadbeef');
		require('net').createServer().listen(0, function() {
			this.close();
		});
	});
});
